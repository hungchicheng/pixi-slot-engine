export const SLOT_CONFIG = {
  COLUMNS: 3,
  ROWS: 3,
  SYMBOL_SIZE: 100,
  SPACING: 20,
  SCROLL_SPEED: 3, // Pixels per frame
  TILES_PER_COLUMN: 6, // Visible rows + extra for seamless scrolling
} as const

